/**
 * Email template types enum.
 * Defines the different types of email templates available.
 */
export enum EmailTemplate {
    VERIFY_EMAIL = 'verify_email',
    WELCOME = 'welcome',
    PASSWORD_RESET = 'password_reset',
    ACCOUNT_ACTIVATED = 'account_activated',
}
