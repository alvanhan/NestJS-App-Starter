/**
 * Email priority levels enum.
 * Defines the priority levels for email notifications.
 */
export enum EmailPriority {
    LOW = 'low',
    NORMAL = 'normal',
    HIGH = 'high',
    URGENT = 'urgent',
}
